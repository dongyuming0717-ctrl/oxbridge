{
  "installCommand": "npm --prefix client install --legacy-peer-deps",
  "buildCommand": "npm --prefix client install --legacy-peer-deps && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!assets/|question-images/).*)", "destination": "/index.html" }
  ]
}
